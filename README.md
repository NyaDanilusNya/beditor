# beditor

### Simple byte editor

---

### Build

```
$ mkdir bin
$ make
```

The result can be found in the `bin/` directory.

### Keymap

- `a` - append
- `i` - insert
- `r` - replace
- `d` - delete
- `w` - write file
- `m` - change mode
- `^C` - quit
- *arrows* - move
